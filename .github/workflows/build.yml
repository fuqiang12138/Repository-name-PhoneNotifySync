name: Build PhoneNotifySync

on:
  workflow_dispatch:
  push:
    branches: [ main ]

jobs:
  windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'
      - name: Build Windows EXE
        run: dotnet publish WindowsNotifyReceiver/WindowsNotifyReceiver.csproj -c Release -r win-x64 --self-contained true -p:PublishSingleFile=true -o publish/windows
      - uses: actions/upload-artifact@v4
        with:
          name: PhoneNotify-Windows
          path: publish/windows/PhoneNotifyReceiver.exe

  android:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - uses: android-actions/setup-android@v3
      - name: Install Android SDK
        run: sdkmanager "platforms;android-35" "build-tools;35.0.0"
      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.7'
      - name: Generate Gradle wrapper
        working-directory: AndroidPhoneNotify
        run: gradle wrapper --gradle-version 8.7
      - name: Build APK
        working-directory: AndroidPhoneNotify
        run: ./gradlew assembleDebug
      - uses: actions/upload-artifact@v4
        with:
          name: PhoneNotify-APK
          path: AndroidPhoneNotify/app/build/outputs/apk/debug/app-debug.apk
